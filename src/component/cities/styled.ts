import { css } from 'emotion';
import styled from 'react-emotion';

export const StyledTableContainer = styled('div')({
  display: 'flex',
  alignItems: 'center',
  color: '#535F67',
  fontSize: 15
});
export const StyledTable = styled('td')(({ }) => ({

  width: 400,

}));